// Generated by CoffeeScript 1.8.0
var Task, americano;

americano = require('americano');

module.exports = Task = americano.getModel('Tasky', {
  'done': {
    type: Boolean,
    "default": false
  },
  'creationDate': {
    type: Date,
    "default": Date.now
  },
  'completionDate': {
    type: Date,
    "default": null
  },
  'description': String,
  'order': Number,
  'tags': {
    type: JSON
  },
  'isArchived': {
    type: Boolean,
    "default": false
  }
});

Task.all = function(callback) {
  return Task.request('all', {}, function(err, tasks) {
    err = err || tasks.error;
    return callback(err, tasks);
  });
};

Task.allInInterval = function(options, callback) {
  return Task.request('byOrder', options, function(err, tasks) {
    err = err || tasks.error;
    return callback(err, tasks);
  });
};

Task.allNotArchived = function(callback) {
  var params;
  params = {
    keys: [false, null]
  };
  return Task.request('byArchiveState', params, function(err, tasks) {
    err = err || tasks.error;
    return callback(err, tasks);
  });
};

Task.allArchived = function(callback) {
  var params;
  params = {
    key: true
  };
  return Task.request('byArchiveState', params, function(err, tasks) {
    err = err || tasks.error;
    return callback(err, tasks);
  });
};
