// Generated by CoffeeScript 1.8.0
var FavoriteTag, americano;

americano = require('americano');

module.exports = FavoriteTag = americano.getModel('FavoriteTag', {
  'label': String,
  'application': String
});

FavoriteTag.allForTasky = function(callback) {
  return FavoriteTag.request('allByApp', {
    key: 'tasky'
  }, function(err, tags) {
    var labels;
    err = err || tags.error;
    if (err != null) {
      return callback(err);
    } else {
      labels = tags.map(function(tag) {
        return tag.label;
      });
      return callback(null, labels);
    }
  });
};

FavoriteTag.ByLabelForTasky = function(label, callback) {
  var options;
  options = {
    key: ['tasky', label]
  };
  return FavoriteTag.request('byAppByLabel', options, callback);
};
