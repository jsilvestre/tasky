#!/bin/bash
node_modules/.bin/stylus \
    -w \
    --include-css \
    --sourcemap \
    --sourcemap-inline \
    --use ./node_modules/nib/lib/nib \
    client/src/styles/application.styl \
    -o client/public/styles
